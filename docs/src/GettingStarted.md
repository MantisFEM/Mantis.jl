# Getting Started

## Installing `Mantis`

Mantis.jl is a registered Julia package. As such, we can easily install `Mantis` using
Julia's package manager `Pkg`, as shown below:

```julia
using Pkg

Pkg.add("Mantis")
```
