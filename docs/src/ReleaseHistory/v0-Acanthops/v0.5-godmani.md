# v0.5 - Acanthops godmani

## New in `v0.5`

  - The `CartesianPoints` structure now allows you to specify a different iteration order.
  - `Mantis` now has a `Makie` extension, instead of a `GLMakie` extension.
